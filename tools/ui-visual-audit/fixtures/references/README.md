# Committable Reference Baselines

Place repository-approved PNG/JPEG UI reference baselines below this directory. Binary images here are Git LFS objects and remain development-only because Android packages only `project/assets/`.

Do not put private, unlicensed, or temporary user inputs here. Those belong under the ignored `summary/ui-visual-audit/` tree.
